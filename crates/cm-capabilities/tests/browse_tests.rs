//! Capability tests for browse filter construction and pagination.
//!
//! These tests exercise `cm_capabilities::browse::browse()` directly against a
//! real SQLite store while keeping each behavior group in a focused module.

mod browse;
mod common;
